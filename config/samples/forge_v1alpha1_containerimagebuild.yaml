apiVersion: forge.dominodatalab.com/v1alpha1
kind: ContainerImageBuild
metadata:
  name: containerimagebuild-sample
spec:
  build:
    # Add registry details here
    imageURL: quay.io/domino/build
    commands:
      - FROM debian
      - RUN apt-get update && apt-get install -y curl
      - RUN echo 'all done'
      - CMD ["/bin/bash"]
