apiVersion: forge.dominodatalab.com/v1alpha1
kind: ContainerImageBuild
metadata:
  name: containerimagebuild-sample
spec:
  build:
    pushRegistry: docker-registry:5000
    imageName: domino/env-build
    commands:
      - FROM debian
      - RUN apt-get update && apt-get install -y curl
      - RUN echo 'all done'
      - CMD ["/bin/bash"]
